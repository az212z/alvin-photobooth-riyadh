const root=document.documentElement;const en=root.lang==='en';
if(!matchMedia('(prefers-reduced-motion: reduce)').matches){root.classList.add('js-motion');const observer=new IntersectionObserver(items=>items.forEach(item=>{if(item.isIntersecting){item.target.classList.remove('wait');observer.unobserve(item.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>{el.classList.add('wait');observer.observe(el)})}
const form=document.querySelector('#enquiry');form.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;const values=new FormData(form);let phone=form.dataset.phone;if(form.dataset.kind==='perla'&&values.get('service')==='بيرلا الجبيلة')phone='966538890090';const title=en?'Hello, I would like to enquire about ':'مرحبًا، أرغب بالاستفسار لدى ';const lines=[title+form.dataset.name];for(const [key,value] of values){const field=form.elements.namedItem(key);const label=field.closest('.form-row').querySelector('label').textContent;lines.push(label+': '+value)}location.href='https://wa.me/'+phone+'?text='+encodeURIComponent(lines.join('\n'))});
const dialog=document.querySelector('#photo-dialog');const photos=[...document.querySelectorAll('.photo')];let current=0;let lastTrigger;function showPhoto(i){current=(i+photos.length)%photos.length;const btn=photos[current];dialog.querySelector('img').src=btn.dataset.full;dialog.querySelector('img').alt=btn.dataset.caption;dialog.querySelector('.dialog-title').textContent=btn.dataset.caption;dialog.querySelector('.dialog-count').textContent=(current+1)+' / '+photos.length}photos.forEach((button,index)=>button.addEventListener('click',()=>{lastTrigger=button;showPhoto(index);dialog.showModal()}));dialog.querySelector('.dialog-close').addEventListener('click',()=>dialog.close());dialog.querySelector('[data-step="-1"]').addEventListener('click',()=>showPhoto(current-1));dialog.querySelector('[data-step="1"]').addEventListener('click',()=>showPhoto(current+1));dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});dialog.addEventListener('close',()=>lastTrigger?.focus());dialog.addEventListener('keydown',e=>{if(e.key==='ArrowRight'){e.preventDefault();showPhoto(current+1)}if(e.key==='ArrowLeft'){e.preventDefault();showPhoto(current-1)}});
document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));photos.forEach(p=>p.hidden=button.dataset.filter!=='all'&&p.dataset.category!==button.dataset.filter)}));
for(const input of document.querySelectorAll('input[type=date]')){const d=new Date();input.min=d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}
